import { Sprite } from "./sprite";

export class Lily extends Sprite {
    constructor(r:number, t:number, speed:number) {
        super(r, t, speed);
    }

    update(time: number) {
        super.update(time);
    }
}
